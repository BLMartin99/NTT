LIB "split_list.sing";

ring r = integer, (x), lp;

list return_list1 = split_list([1,2,3,4,5,6,7,8]);

// Working case
if(return_list1[1] != [1,3,5,7])
{
    print("split_list FAILED");
    quit;
}

if(return_list1[0] != [2,4,6,8])
{
    print("split_list FAILED");
    quit;
}

// Failing case -- should fail
list return_list2 = split_list([1,2,3,4,5,6,7]);
